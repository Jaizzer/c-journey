#include <stdio.h>

int main(void) {
    int a;
    int b;
    float c;
    float d;

    printf("Inputs: ");
    scanf("%d%d%f%f", &a, &b, &c, & d);
    printf("%d %d %f %f\n", a, b, c, d);

    return 0;
}